>rand_prog 3~
if rand (50)
	pmote circles overhead, shouting shrill cries.
else
	pmote swoops into the waves, diving for a fish.
endif
~
>greet_prog 5~
if rand (90)
	pmote swoops down, and attacks a fish!
	kill fish
else
	pmote circles overhead, watching for fish.
endif
~
|

