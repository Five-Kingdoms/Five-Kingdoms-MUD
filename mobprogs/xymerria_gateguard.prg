>greet_prog 15~
if rand(25)
	pmote steps aside and allow you to pass $k.
else
	if rand(60)
		say I'd suggest staying out of the K'Dan, $n.
	else
		pmote examines $r carefully.
	endif
endif
~
>rand_prog 5~
pmote peers around $k in search of criminals.
~
|
