>rand_prog 15~
if rand(33)
	pmote looks up briefly, then goes back to $k paper work.
else
	if rand(50)
		pmote scribbles something on the paper $j has before $k.
	else
		pmote stands up, stretches, then goes back to $k paper work.
	endif
endif
~
|
